.DS_Store
CLAUDE.md
.claude/
build/

# Camera RAW / large test images — never commit (calibration uses JPEG)
*.CR3
*.CR2
*.ARW
*.NEF
*.RAF
*.DNG
*.tif
*.tiff

# Calibration photo sets (large JPEG folders) — keep the JSON library, not the shots
calibrations/*/
calibrations/tofdbg-*.json
